type CategoryDef = {
    slug: string,
    name: string,
    url: string
}

type AdContentDef = {
    title: string, 
    para: string, 
    imageUrl: string, 
    prodTitle: string
}

export type {
    CategoryDef,
    AdContentDef
}